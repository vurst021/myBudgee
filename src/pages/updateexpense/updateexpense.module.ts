import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { UpdateexpensePage } from './updateexpense';

@NgModule({
  declarations: [
    UpdateexpensePage,
  ],
  imports: [
    IonicPageModule.forChild(UpdateexpensePage),
  ],
})
export class UpdateexpensePageModule {}
